---
import { Image } from "astro:assets";
import Badges from "../Badges.svelte";
import Button from "../../components/ui/Button.svelte";
import { getTranslator, getLink } from "../../lib/i18n.ts";

const __ = await getTranslator(Astro.currentLocale);
---

<div class="bg-linear-to-b from-[#FFFEF9] to-[#FFFFFB] py-10 sm:py-20">
  <div class="container">
    <img
      src="/logo.svg"
      alt="Gato-International logo"
      width="48"
      height="48"
      class="mx-auto mb-6"
    />
    <h2 class="text-h3 sm:text-h2 mb-3 text-center font-medium">
      {__("Onze druktechnieken")}
    </h2>
    <p class="text-muted-foreground mb-12 text-center text-lg">
      {__("Van zeefdruk tot laser graveren. Elke techniek perfect voor uw product.")}<br />
      {__("Niet gevonden wat u zoekt? Neem contact op, we denken graag met u mee.")}
    </p>

    <Badges
      count={10}
      countMobile={9}
      client:visible
      class="mx-auto mb-13 grid max-w-[660px] grid-cols-3 justify-center justify-items-center gap-2 gap-y-10 sm:grid-cols-5 sm:gap-x-10"
    />

    <Button
      rel="nofollow noreferrer"
      href={getLink(Astro, "/contact")}
      size="lg"
      class="mx-auto flex w-max"
    >
      {__("Vraag offerte aan")}
    </Button>
  </div>
</div>
